const login = require('./loginController');
const products = require('./productsController');
const user = require('./userController');
const sales = require('./salesController');

module.exports = { login, products, user, sales };
